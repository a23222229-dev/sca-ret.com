---
import Layout from '../../layouts/Layout.astro';
---
<Layout title="表單已送出" description="表單已填寫完成並送出">
  <section class="banner">
    <h1>表單已填寫完成並送出</h1>
  </section>

  <section class="body">
    <p>收到電子郵件報價單後，您便會有訂單編號，再煩請匯款，資訊如下</p>
    <dl>
      <dt>戶名</dt>
      <dd>詠業商略顧問有限公司</dd>
      <dt>銀行</dt>
      <dd>永豐銀行（807）南台中分行</dd>
      <dt>帳號</dt>
      <dd>997137＋您的訂單編號後8碼</dd>
    </dl>
    <p class="example">範例：電子郵件的訂單編號為SCA-20250123456，專屬帳號即為997137+50123456</p>
    <p>將匯款截圖回傳至「詠業商略」官方帳號。</p>
    <a class="line-button" href="#" rel="noopener">轉至詠業商略LINE官方帳號</a>
  </section>
</Layout>

<style>
  .banner {
    background: linear-gradient(135deg, var(--brand), var(--brand-deep));
    color: var(--on-brand);
    text-align: center;
    padding: 2rem;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    margin-bottom: 2.5rem;
  }
  .banner h1 { margin: 0; font-size: var(--text-2xl); color: var(--on-brand); }

  .body { max-width: 560px; margin: 0 auto; text-align: center; }
  .body p { color: var(--ink-soft); }
  .example { font-size: var(--text-sm); }

  dl {
    text-align: left;
    display: inline-block;
    margin: 1.5rem 0;
  }
  dt { font-size: var(--text-sm); color: var(--ink-soft); margin-top: 0.75rem; }
  dd { font-size: var(--text-lg); margin: 0.15rem 0 0; }

  .line-button {
    display: inline-block;
    margin-top: 1.5rem;
    background: var(--brand);
    color: var(--on-brand);
    text-decoration: none;
    font-weight: 700;
    padding: 0.9rem 2rem;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow);
    transition: transform var(--transition), box-shadow var(--transition);
  }
  .line-button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
</style>
